# Discussion Questions
### Why does read() return int instead of byte?
#### What would break if it returned byte?
### When did Task 4 work vs. when did it fail?
#### What kinds of files can be interpreted as ASCII?
#### What happens with non-ASCII bytes (128-255)?
### Why is try-with-resources better than try-catch-finally?
#### What could go wrong with manual closing?
### Is this how you'd read text files in real programs?
